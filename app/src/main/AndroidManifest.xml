<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.yumm">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.RecipeCardDetailActivity">
        </activity>
        <activity android:name=".view.RecipeCardDetailStepActivity">
        </activity>
        <activity android:name=".view.RecipeCardIngredientActivity">
        </activity>

        <receiver android:name=".widgets.YummWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/yumm_widget_provider_info"/>
        </receiver>

        <!-- Recipe Content Provider -->
        <provider
            android:authorities="com.example.android.yumm"
            android:name="com.example.android.yumm.provider.RecipeContentProvider"
            android:exported="true"/>

        <!-- For grid view in widget -->
        <service android:name=".widgets.YummGridWidgetService"
                 android:permission="android.permission.BIND_REMOTEVIEWS"/>
    </application>

</manifest>